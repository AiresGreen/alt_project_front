import { recommendationsList } from "@/fakers/recommendation";
import axios from "axios";

/*get all options*/
export async function getAllRecommendation() {
  try {
    // const data = recommendationsList;
    return data;
  } catch (error) {
    throw new Error();
  }
}